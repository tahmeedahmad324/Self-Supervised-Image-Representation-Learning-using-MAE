{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[{"sourceType":"datasetVersion","sourceId":109264,"datasetId":56828,"databundleVersionId":119232}],"dockerImageVersionId":31286,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"import torch, torch.nn as nn, torch.optim as optim, os, numpy as np, matplotlib.pyplot as plt, random, math\nfrom torch.utils.data import Dataset, DataLoader\nfrom torchvision import transforms\nfrom PIL import Image\nfrom tqdm import tqdm\nfrom pytorch_msssim import ssim\n\ndevice = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\nprint(f\"Using device: {device}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def find_tiny_imagenet():\n    base_path = '/kaggle/input'\n    for root, dirs, files in os.walk(base_path):\n        if 'train' in dirs and 'val' in dirs:\n            return root\n    return None\n\nDATA_DIR = find_tiny_imagenet()\nif DATA_DIR:\n    print(f\"Found TinyImageNet at: {DATA_DIR}\")\nelse:\n    raise FileNotFoundError(\"TinyImageNet not found!\")","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class TinyImageNetDataset(Dataset):\n    def __init__(self, root_dir, split='train', transform=None):\n        self.root_dir = os.path.join(root_dir, split)\n        self.transform = transform\n        self.images = []\n        \n        if split == 'train':\n            for class_dir in os.listdir(self.root_dir):\n                class_path = os.path.join(self.root_dir, class_dir, 'images')\n                if os.path.isdir(class_path):\n                    for img_name in os.listdir(class_path):\n                        self.images.append(os.path.join(class_path, img_name))\n        else:\n            val_images = os.path.join(self.root_dir, 'images')\n            if os.path.isdir(val_images):\n                for img_name in os.listdir(val_images):\n                    self.images.append(os.path.join(val_images, img_name))\n    \n    def __len__(self):\n        return len(self.images)\n    \n    def __getitem__(self, idx):\n        img_path = self.images[idx]\n        image = Image.open(img_path).convert('RGB')\n        \n        if self.transform:\n            image = self.transform(image)\n        \n        return image\n\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"transform = transforms.Compose([\n    transforms.Resize((IMG_SIZE, IMG_SIZE)),\n    transforms.ToTensor(),\n    transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])\n])\n\ntrain_dataset = TinyImageNetDataset(DATA_DIR, split='train', transform=transform)\ntrain_loader = DataLoader(train_dataset, batch_size=BATCH_SIZE, shuffle=True, num_workers=2)\n\nprint(f\"Training samples: {len(train_dataset)}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def patchify(images, patch_size):\n    B, C, H, W = images.shape\n    num_patches_h = H // patch_size\n    num_patches_w = W // patch_size\n    \n    patches = images.reshape(B, C, num_patches_h, patch_size, num_patches_w, patch_size)\n    patches = patches.permute(0, 2, 4, 3, 5, 1)\n    patches = patches.reshape(B, num_patches_h * num_patches_w, patch_size * patch_size * C)\n    \n    return patches\n\n\ndef unpatchify(patches, patch_size, img_size):\n    B, num_patches, _ = patches.shape\n    C = 3\n    num_patches_side = img_size // patch_size\n    \n    patches = patches.reshape(B, num_patches_side, num_patches_side, patch_size, patch_size, C)\n    patches = patches.permute(0, 5, 1, 3, 2, 4)\n    images = patches.reshape(B, C, img_size, img_size)\n    \n    return images\n\n\ndef random_masking(x, mask_ratio):\n    B, N, D = x.shape\n    len_keep = int(N * (1 - mask_ratio))\n    \n    noise = torch.rand(B, N, device=x.device)\n    ids_shuffle = torch.argsort(noise, dim=1)\n    ids_restore = torch.argsort(ids_shuffle, dim=1)\n    \n    ids_keep = ids_shuffle[:, :len_keep]\n    x_masked = torch.gather(x, dim=1, index=ids_keep.unsqueeze(-1).repeat(1, 1, D))\n    \n    mask = torch.ones([B, N], device=x.device)\n    mask[:, :len_keep] = 0\n    mask = torch.gather(mask, dim=1, index=ids_restore)\n    \n    return x_masked, mask, ids_restore, ids_keep\n\n\nsample_img = next(iter(train_loader))\nprint(f\"Original image shape: {sample_img.shape}\")\n\npatches = patchify(sample_img, PATCH_SIZE)\nprint(f\"Patches shape: {patches.shape}\")\n\nreconstructed = unpatchify(patches, PATCH_SIZE, IMG_SIZE)\nprint(f\"Reconstructed shape: {reconstructed.shape}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class MultiHeadAttention(nn.Module):\n    def __init__(self, dim, num_heads):\n        super().__init__()\n        self.num_heads = num_heads\n        self.head_dim = dim // num_heads\n        self.scale = self.head_dim ** -0.5\n        self.q_proj = nn.Linear(dim, dim)\n        self.k_proj = nn.Linear(dim, dim)\n        self.v_proj = nn.Linear(dim, dim)\n        self.out_proj = nn.Linear(dim, dim)\n    \n    def forward(self, x):\n        batch, seq_len, embed_dim = x.shape\n        \n        q = self.q_proj(x)\n        k = self.k_proj(x)\n        v = self.v_proj(x)\n        \n        q = q.view(batch, seq_len, self.num_heads, self.head_dim).transpose(1, 2)\n        k = k.view(batch, seq_len, self.num_heads, self.head_dim).transpose(1, 2)\n        v = v.view(batch, seq_len, self.num_heads, self.head_dim).transpose(1, 2)\n        \n        attn_scores = (q @ k.transpose(-2, -1)) * self.scale\n        attn_weights = attn_scores.softmax(dim=-1)\n        out = attn_weights @ v\n        \n        out = out.transpose(1, 2).reshape(batch, seq_len, embed_dim)\n        out = self.out_proj(out)\n        \n        return out\n\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class FeedForward(nn.Module):\n    def __init__(self, dim, hidden_dim):\n        super().__init__()\n        self.fc1 = nn.Linear(dim, hidden_dim)\n        self.act = nn.GELU()\n        self.fc2 = nn.Linear(hidden_dim, dim)\n    \n    def forward(self, x):\n        x = self.fc1(x)\n        x = self.act(x)\n        x = self.fc2(x)\n        return x\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class TransformerBlock(nn.Module):\n    def __init__(self, dim, num_heads, mlp_ratio=4.0):\n        super().__init__()\n        hidden_dim = int(dim * mlp_ratio)\n        \n        self.norm1 = nn.LayerNorm(dim)\n        self.attn = MultiHeadAttention(dim, num_heads)\n        self.norm2 = nn.LayerNorm(dim)\n        self.mlp = FeedForward(dim, hidden_dim)\n    \n    def forward(self, x):\n        x = x + self.attn(self.norm1(x))\n        x = x + self.mlp(self.norm2(x))\n        return x\n\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class ViTEncoder(nn.Module):\n    def __init__(self, img_size, patch_size, in_chans, embed_dim, depth, num_heads):\n        super().__init__()\n        num_patches = (img_size // patch_size) ** 2\n        self.patch_embed = nn.Linear(patch_size * patch_size * in_chans, embed_dim)\n        self.pos_embed = nn.Parameter(torch.zeros(1, num_patches, embed_dim))\n        \n        # Transformer blocks\n        self.blocks = nn.ModuleList([\n            TransformerBlock(embed_dim, num_heads) for _ in range(depth)\n        ])\n        \n        self.norm = nn.LayerNorm(embed_dim)\n    \n    def forward(self, x, pos_indices=None):\n        batch, seq_len, embed_dim = x.shape\n        x = self.patch_embed(x)\n\n        if pos_indices is not None:\n            pos_embed = torch.gather(\n                self.pos_embed.expand(batch, -1, -1),\n                dim=1,\n                index=pos_indices.unsqueeze(-1).expand(-1, -1, x.shape[-1])\n            )\n            x = x + pos_embed\n        else:\n            x = x + self.pos_embed[:, :N, :]\n            \n        for block in self.blocks:\n            x = block(x)\n        x = self.norm(x)\n        return x","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"class ViTDecoder(nn.Module):\n    def __init__(self, num_patches, patch_size, in_chans, embed_dim, depth, num_heads, encoder_embed_dim):\n        super().__init__()\n        self.num_patches = num_patches\n        self.patch_size = patch_size\n        self.in_chans = in_chans\n        self.decoder_embed = nn.Linear(encoder_embed_dim, embed_dim)\n        self.mask_token = nn.Parameter(torch.zeros(1, 1, embed_dim))\n        self.pos_embed = nn.Parameter(torch.zeros(1, num_patches, embed_dim))\n        self.blocks = nn.ModuleList([\n            TransformerBlock(dim=embed_dim, num_heads=num_heads) \n            for _ in range(depth)\n        ])\n        self.norm = nn.LayerNorm(embed_dim)\n        self.pred = nn.Linear(embed_dim, patch_size * patch_size * in_chans)\n    \n    def forward(self, x, ids_restore):\n        x = self.decoder_embed(x)\n        batch, num_visible, D = x.shape\n        mask_tokens = self.mask_token.repeat(batch, ids_restore.shape[4] - num_visible, 1)\n        x_full = torch.cat([x, mask_tokens], dim=1)\n        x_full = torch.gather(x_full, dim=1, index=ids_restore.unsqueeze(-1).repeat(1, 1, D))\n        x_full = x_full + self.pos_embed\n        for block in self.blocks:\n            x_full = block(x_full)\n        x_full = self.norm(x_full)\n        pred = self.pred(x_full)\n        \n        return pred","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"PATCH_SIZE = 16\nIMG_SIZE = 224\nNUM_PATCHES = (IMG_SIZE // PATCH_SIZE) ** 2\nMASK_RATIO = 0.75\n\n# Encoder (ViT-Base)\nENC_DIM = 768\nENC_DEPTH = 12\nENC_HEADS = 12\n\n# Decoder (ViT-Small)\nDEC_DIM = 384\nDEC_DEPTH = 8\nDEC_HEADS = 6\n\nBATCH_SIZE = 32\nLEARNING_RATE = 1.5e-4\nEPOCHS = 50","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Cell 13: Masked Autoencoder\nclass MaskedAutoencoder(nn.Module):\n    def __init__(self, img_size, patch_size, in_chans,\n                 encoder_embed_dim, encoder_depth, encoder_num_heads,\n                 decoder_embed_dim, decoder_depth, decoder_num_heads,\n                 mask_ratio):\n        super().__init__()\n        \n        self.patch_size = patch_size\n        self.mask_ratio = mask_ratio\n        num_patches = (img_size // patch_size) ** 2\n        \n        # Encoder\n        self.encoder = ViTEncoder(\n            img_size=img_size,\n            patch_size=patch_size,\n            in_chans=in_chans,\n            embed_dim=encoder_embed_dim,\n            depth=encoder_depth,\n            num_heads=encoder_num_heads\n        )\n        \n        # Decoder\n        self.decoder = ViTDecoder(\n            num_patches=num_patches,\n            patch_size=patch_size,\n            in_chans=in_chans,\n            embed_dim=decoder_embed_dim,\n            depth=decoder_depth,\n            num_heads=decoder_num_heads,\n            encoder_embed_dim=encoder_embed_dim\n        )\n    \n    def forward(self, imgs):\n        patches = patchify(imgs, self.patch_size)\n        patches_masked, mask, ids_restore, ids_keep = random_masking(patches, self.mask_ratio)\n        latent = self.encoder(patches_masked, pos_indices=ids_keep)\n        pred = self.decoder(latent, ids_restore)\n        \n        return pred, mask, patches\n\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Cell 14: Initialize Model\nmodel = MaskedAutoencoder(\n    img_size=IMG_SIZE,\n    patch_size=PATCH_SIZE,\n    in_chans=3,  # Add this!\n    encoder_embed_dim=ENC_DIM,\n    encoder_depth=ENC_DEPTH,\n    encoder_num_heads=ENC_HEADS,\n    decoder_embed_dim=DEC_DIM,\n    decoder_depth=DEC_DEPTH,\n    decoder_num_heads=DEC_HEADS,\n    mask_ratio=MASK_RATIO\n).to(device)\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Cell 15: Loss Function\ndef mae_loss(pred, target, mask):\n    loss = (pred - target) ** 2\n    loss = loss.mean(dim=-1)\n    loss = (loss * mask).sum() / mask.sum()\n    \n    return loss","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"optimizer = optim.AdamW(model.parameters(), lr=LEARNING_RATE, weight_decay=0.05)\nscheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=EPOCHS)\nscaler = torch.cuda.amp.GradScaler()\n\ndef train_epoch(model, loader, optimizer, scaler, device):\n    model.train()\n    total_loss = 0\n    \n    for batch in tqdm(loader, desc=\"Training\"):\n        images = batch.to(device)\n        optimizer.zero_grad()\n        with torch.cuda.amp.autocast():\n            pred, mask, target = model(images)\n            loss = mae_loss(pred, target, mask)\n        scaler.scale(loss).backward()\n        scaler.step(optimizer)\n        scaler.update() \n        total_loss += loss.item()\n    \n    return total_loss / len(loader)\n\nplt.figure(figsize=(10, 5))\nplt.plot(train_losses, marker='o', label='Training Loss')\nplt.xlabel('Epoch')\nplt.ylabel('Loss')\nplt.title('MAE Training Loss')\nplt.legend()\nplt.grid(True, alpha=0.3)\nplt.savefig('training_loss.png')\nplt.show()","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"train_losses = []\nbest_loss = float('inf')\n\nfor epoch in range(EPOCHS):\n    print(f\"\\n--- Epoch {epoch+1}/{EPOCHS} ---\")\n    avg_loss = train_epoch(model, train_loader, optimizer, scaler, device)\n    train_losses.append(avg_loss)\n    scheduler.step()\n    print(f\"Training Loss: {avg_loss:.4f}\")\n    \n    if avg_loss < best_loss:\n        best_loss = avg_loss\n        torch.save(model.state_dict(), 'mae_best_model.pth')\n        print(\"Saved new best model!\")\n\nprint(\"Training complete!\")","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def visualize_reconstruction(model, images, device, num_samples=5):\n    model.eval()\n    \n    with torch.no_grad():\n        images = images[:num_samples].to(device)\n        pred, mask, target = model(images)\n        mean = torch.tensor([0.485, 0.456, 0.406]).view(1, 3, 1, 1).to(device)\n        std = torch.tensor([0.229, 0.224, 0.225]).view(1, 3, 1, 1).to(device)\n        \n        pred_imgs = unpatchify(pred, PATCH_SIZE, IMG_SIZE)\n        pred_imgs = pred_imgs * std + mean\n        pred_imgs = torch.clamp(pred_imgs, 0, 1)\n        \n        orig_imgs = images * std + mean\n        orig_imgs = torch.clamp(orig_imgs, 0, 1)\n        \n        mask_expanded = mask.unsqueeze(-1).repeat(1, 1, PATCH_SIZE**2 * 3)\n        masked_patches = target * (1 - mask_expanded)\n        masked_imgs = unpatchify(masked_patches, PATCH_SIZE, IMG_SIZE)\n        masked_imgs = masked_imgs * std + mean\n        masked_imgs = torch.clamp(masked_imgs, 0, 1)\n        \n        fig, axes = plt.subplots(num_samples, 3, figsize=(12, num_samples*4))\n        \n        for i in range(num_samples):\n            axes[i, 0].imshow(masked_imgs[i].cpu().permute(1, 2, 0))\n            axes[i, 0].set_title(\"Masked (75%)\")\n            axes[i, 0].axis('off')\n            \n            axes[i, 1].imshow(pred_imgs[i].cpu().permute(1, 2, 0))\n            axes[i, 1].set_title(\"Reconstruction\")\n            axes[i, 1].axis('off')\n            \n            axes[i, 2].imshow(orig_imgs[i].cpu().permute(1, 2, 0))\n            axes[i, 2].set_title(\"Original\")\n            axes[i, 2].axis('off')\n        \n        plt.tight_layout()\n        plt.show()\n\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"sample_batch = next(iter(train_loader))\nvisualize_reconstruction(model, sample_batch, device, num_samples=5)","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def calculate_psnr(img1, img2):\n    mse = torch.mean((img1 - img2) ** 2)\n    if mse == 0:\n        return float('inf')\n    psnr = -10.0 * torch.log10(mse)\n    return psnr.item()\n\ndef calculate_ssim(img1, img2):\n    ssim_val = ssim(img1, img2, data_range=1.0, size_average=True)\n    \n    return ssim_val.item()\n","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"def evaluate_metrics(model, loader, device, num_batches=10):\n    model.eval()\n    psnr_scores = []\n    ssim_scores = []\n\n    with torch.no_grad():\n        for i, batch in enumerate(loader):\n            if i >= num_batches:\n                break\n                \n            images = batch.to(device)\n            with torch.cuda.amp.autocast():\n                pred_patches, mask, target_patches = model(images)\n                \n            pred_images = unpatchify(pred_patches)\n            target_images = unpatchify(target_patches)\n            \n            pred_images = torch.clamp(pred_images, 0.0, 1.0)\n            \n            batch_psnr = calculate_psnr(pred_images, target_images)\n            batch_ssim = calculate_ssim(pred_images, target_images)\n            \n            psnr_scores.append(batch_psnr)\n            ssim_scores.append(batch_ssim)\n            \n    return sum(psnr_scores) / len(psnr_scores), sum(ssim_scores) / len(ssim_scores)\n\n\npsnr_avg, ssim_avg = evaluate_metrics(model, val_loader, device)\nprint(f\"PSNR: {psnr_avg:.2f}\")\nprint(f\"SSIM: {ssim_avg:.4f}\")","metadata":{"trusted":true},"outputs":[],"execution_count":null}]}